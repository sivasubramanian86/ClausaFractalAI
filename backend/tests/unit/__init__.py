"""Unit test package for ClausaFractalAI backend.

Tests in this package are fully isolated — no network calls, no database,
no LLM inference. Every external dependency is mocked or stubbed.

Coverage target: 100% statement and branch coverage.
"""
